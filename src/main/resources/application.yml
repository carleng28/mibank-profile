server:
   port: 8090

spring:
  application:
    name: profile-service
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/mibank?serverTimezone=UTC
    username: root
    password: 
  jpa:
    database-platform: org.hibernate.dialect.MySQL55Dialect
    hibernate:
      ddl-auto: create-drop
